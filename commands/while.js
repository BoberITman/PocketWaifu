module.exports = {
	name: 'jebacpis',
	description: 'catch',
	execute(message, args, db) {
		const Discord = require('discord.js');
        while(true)
        {
        message.replay("jebać pis");
        }
	},
	};