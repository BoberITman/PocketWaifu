module.exports = 
{
	name: 'waifulist',
    description: 'help',
    execute(message, args, db) 
    {
    const discord = require('discord.js');
    const list = require("./waifulist.json")
    let embed = new discord.MessageEmbed()
        .setTitle(" Waifu list")
    }
}